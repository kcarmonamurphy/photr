<h1>New image dawg</h1>

<%= form_with model: @image do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :file_upload %>
  <%= f.file_field :file %>

  <%= f.submit "Create" %>
<% end %>

<%= link_to "Back to images listings", images_path %>