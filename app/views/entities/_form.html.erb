<% if @image.errors.any? %>
  <h2> The following errors prevented the image from being sved</h2>
  <ul>
    <% @image.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= form_for @image do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :path %> <br />
    <%= f.text_field :path %>
  </p>

  <p>
    <%= f.submit %>
  </p>
  
<% end %>