<EmberTable as |t|>
  <t.head
    @columns={{columns}}
    @widthConstraint={{widthConstraint}}
    @resizeMode={{if (eq widthConstraint 'eq-container') 'fluid' 'standard'}}
   />
  <t.body @rows={{rows}} />
</EmberTable>